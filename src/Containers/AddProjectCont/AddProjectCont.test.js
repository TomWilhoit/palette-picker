import React from "react";
import { shallow } from "enzyme";
import { AddProjectCont } from "./AddProjectCont";

describe("AddProjectCont", () => {
  it("should match snapshot", () => {
    
  });
});